import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2', // Color azul por defecto de MUI
    },
    secondary: {
      main: '#dc004e',
    },
  },
});

export default theme;
